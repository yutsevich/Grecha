export type CheckboxImagedProps = {
	isChecked: boolean;
	onChange : (isChecked: boolean) => void;
	imageOn  : string;
	imageOff : string;
	alt?     : string;
};

// export type CheckboxCustomState = {
// };
